
<div class="container">
  <br /> <br />
 <h3><font color="#569AAB">New Attachment</font></h3><br>
  <div class="row">
<div class="col-lg-6"> 
<%= bootstrap_form_for(@attachment, local: true,:html => { :multipart => true },url: {action: "createattachment"}) do |form| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
     <%= hidden_field_tag(:project_id,value = @project.id) %>

   <div class="form-group">
	    <%= form.file_field :file %>
 	 </div>

     <div class="form-group">
    <div class="actions">
      <%= form.submit %>
    </div>
  </div>

  <% end %>
  </div>
  </div>
  <div> 